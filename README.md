# AM_project

## Create and start the virtual enviroment
```bash
# Run only once this command for creating the virtual env
python3 -m venv myenv

# Activate the virtual env
myenv\Scripts\activate.bat
```